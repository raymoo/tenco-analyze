<!-- This is what a new account request looks like in XML -->

<account>

<name>username</name>

<password>password</password>

<mail_address>bob@bob.bob</mail_address> <!-- text may be blank -->

</account>

<!-- When this xml is posted to the right address, the server should create an
     account. The server may choose to send some text back as a message to the
     user. On a successful registration, response code 200 (Ok) should be sent.
     A server should at least allow usernames of up to 32 alphanumeric
     characters plus underscores, passwords between 4 and 16 bytes, and mail
     addresses of up to 256 bytes.
-->
